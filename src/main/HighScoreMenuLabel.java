package main;

import java.awt.Color;

public class HighScoreMenuLabel extends MenuLabel {

    public HighScoreMenuLabel(
        String text,
        String fontType,
        int fontStyle,
        float fontSize,
        Color color,
        float x,
        float y,
        String id) 
    {
        super(text, fontType, fontStyle, fontSize, color, x, y, id);
    }

}
